#ifndef CONSTANTS_H
#define CONSTANTS_H

const int PLAYER_MOVE_SPEED = 5;
const int ENERGY_UPDATE_INTERVAL = 100; // Milliseconds
const float ENERGY_DRAIN_RATE = 80.0f / 60.0f; // 80% per minute
const float ENERGY_GAIN_RATE = 80.0f / 60.0f; // 80% per minute
const int ENEMY_DAMAGE = 20;
const int ENEMY_SPAWN_INTERVAL = 500; // Milliseconds
const float ENEMY_SPEED = 2.0f;
const float ENEMY_SPEED_INCREMENT = 0.05f;

#endif
